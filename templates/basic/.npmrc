# Use exact versions to avoid surprises
save-exact=true

# pnpm strict mode for peer dependencies
strict-peer-dependencies=true

# Prevent hoisting everything to root
shamefully-hoist=false

# Disable npm lockfile (we use pnpm-lock.yaml)
package-lock=false

# Default registry
registry=https://registry.npmjs.org/
